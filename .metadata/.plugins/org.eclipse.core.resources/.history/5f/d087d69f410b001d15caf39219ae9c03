package com.xworkz_collectionTest_set_set_sorted_set;

import java.util.SortedSet;

public class SortedsetDemo {

	public static void main(String[] args) {
		
		SortedSet sortedset =new SortedSet();
		sortedset.add("vannu");//it is not index based
		sortedset.add("vanita");//it is not allowed null vales but allow one single null value
		sortedset.add('v');//this will not on insertion order stored randomly
		sortedset.add("null");//doesn't allow duplicates
		sortedset.add("vanita");
		

	}

}
